<?php
/*
if (!isset($_SESSION)) {
    session_start();
}

// Verifica se a sessão está ativa
if (session_status() == PHP_SESSION_ACTIVE) {
    // Verifica se a chave 'oAluno' existe na array de sessão
    if (array_key_exists('oParticipante', $_SESSION) ||array_key_exists('oProfessor', $_SESSION)  ) {
        //echo 'A variável de sessão "oAluno" existe.';
        // Você pode fazer algo com a variável 'oAluno' aqui, se necessário
    } else {
        //echo 'A variável de sessão "oAluno" não existe.';
//        (new LoginController())->loginPageAction();
    }
} else {
   // echo 'A sessão não está ativa.';
    (new LoginController())->loginPageAction();
}

*/


?>